services:
  - type: web
    name: ping-monitor
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn -c gunicorn.conf.py app:app
    envVars:
      PING_INTERVAL: "10"
      PING_TIMEOUT: "3"