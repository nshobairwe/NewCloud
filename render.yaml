services:
  - type: web
    name: ping-monitor
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn -w 2 -k sync -b 0.0.0.0:$PORT app:app
    envVars:
      PING_INTERVAL: "5"
      PING_TIMEOUT: "2"
    # This ensures when_ready is called
    plan: free